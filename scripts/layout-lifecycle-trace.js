// Lifecycle tracing is implemented in src/renderer.js and enabled with:
// npm start -- --trace-layout-lifecycle
// It emits only panel/model IDs, lifecycle event, origin, and failure code.
module.exports = {};
