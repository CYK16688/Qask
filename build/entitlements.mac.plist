<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Electron defaults: JIT, renderer memory, and library validation for webviews. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Qask records local audio attachments with the microphone. Under the
         hardened runtime this entitlement is required for audio capture; the
         user still has to approve the microphone prompt. -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
  </dict>
</plist>
